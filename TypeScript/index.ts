let 이름: string = 'Min';
let 나이: number = 25;
let 결혼여부: boolean = false;
let 널: null = null;
let 언디파인드: undefined = undefined;
let 회원들: string[] = ['Min', 'Kim']
let 회원정보: { name: string, age: number } = { name: 'Min', age: 25 }
let myName: string = 'MinSungJe'
let myAge: number = 25
let myLocation: string = 'Seoul'
let mySong: { title: string, singer: string } = { title: '예허', singer: '조쿤아' }
let project: { member: string[], days: number, started: boolean } = {
    member: ['kim', 'park'],
    days: 30,
    started: true,
}